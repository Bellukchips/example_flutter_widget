import 'package:flutter/material.dart';

class Looks extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: new Center(
        child: new Column(
          children: <Widget>[new Text("Halaman Looks"), new Icon(Icons.looks)],
        ),
      ),
    );
  }
}
